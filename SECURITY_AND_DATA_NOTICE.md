# Security and data notice

Do not commit raw microscopy images, semantic masks, trained checkpoints, access tokens, local data paths, personal identifiers, or draft publisher files without explicit permission. The `.gitignore` excludes common local artifacts, but review `git status` before every commit.
